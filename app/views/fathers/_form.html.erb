<%= form_with(model: father) do |form| %>
  <% if father.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(father.errors.count, "error") %> prohibited this father from being saved:</h2>

      <ul>
        <% father.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :avatar, style: "display: block" %>
    <%= form.file_field :avatar %>
  </div>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  <div>
    <%= form.label :cpf, style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  <div>
    <%= form.label :email, style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  <div>
    <%= form.label :occupation_id, style: "display: block" %>
    <%= form.collection_select(:occupation_id, @occupations, :id, :description, include_blank: true) %>
  </div>

  
  <div>
    <%= form.submit %>
  </div>
<% end %>
